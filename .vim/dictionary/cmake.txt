project(
add_executable(
add_library(
add_test(
add_subdirectory(
add_definitions(
cmake_minimum_required(
enable_testing()
target_link_libraries(
find_package(
set(
include(
include_directories(
install(
foreach(
endforeach()
if(
endif()
else()

function(
endfunction()
macro(
endmacro()

PROJECT_NAME
CMAKE_CURRENT_LIST_DIR
REQUIRED
VERSION
NAMES
PUBLIC
PRIVATE
PROTECTED
APPEND
PROPERTY
GLOBAL
